GEMINI_API_KEY=your_key_here

# Auth gate credentials (used by the demo login page)
# Set these in .env.local — never commit real credentials to source control
APP_USERNAME=your_username_here
APP_PASSWORD=your_password_here
